To use the prediction function, please run the python server first.

You will have to run your own pgadmin database as well, remember to change the database connection string in appsettings.json.

When you have set up your connection string and database, use "Add-Migration" and "Update-Database" commands to fire EF core.