To run the server install python x64.

Open up the cmd, navigate to that folder and type <python main.py>.
This will start up the server.
If you get exceptions, look at the missing libraries and !pip install them.